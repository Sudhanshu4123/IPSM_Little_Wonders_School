spring.application.name=littlewonders
server.port=${PORT:8080}
spring.thymeleaf.cache=false

# Database Configuration (Production/Server)
# Change 'localhost' to your server's database host IP if it's not on the same server
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/littlewonders?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:ipsm98765@#}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=ipsmlittlewonders@gmail.com
spring.mail.password=biew zxqz xhjs zmsy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.required=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000

# Enquiry recipient email
enquiry.recipient.email=ipsmlittlewonders@gmail.com

# Multi-part File Uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Admin Initial Credentials
admin.username=ipsm.admin.littlewonders@gmail.com
admin.password=ipsmlittlewonders@2026#
